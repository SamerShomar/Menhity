# ---- قاعدة البيانات ----
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/menhity?schema=public"
SHADOW_DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/menhity_shadow?schema=public"

# ---- المصادقة ----
# مفتاح توقيع الجلسات — غيّره في بيئة الإنتاج
AUTH_SECRET="ضع-مفتاحاً-عشوائياً-طويلاً-هنا"

# ---- الذكاء الاصطناعي (اختياري) ----
# بدون مفتاح تعمل الأدوات بوضع المحاكاة (mock) بدل الاتصال بالمزوّد
ANTHROPIC_API_KEY=""

# ---- عام ----
NEXT_PUBLIC_APP_URL="http://localhost:3000"
