# سجل الإصدارات

## v1.6.2 — الأسطح العائمة: تمويه فعّال وشفافية أعلى

**2026-09-17**

قائمة المستخدم كانت تظهر لوحاً رمادياً ضبابياً ونصّ الصفحة خلفها حادّ
تماماً — لا زجاج ولا حجب.

### تمويه ميّت

عنصر فيه `backdrop-filter` يصنع **جذر خلفية**، وما بداخله لا يموّه إلا ما
رُسم داخل ذلك الجذر. والقائمة كانت داخل الترويسة الزجاجية، فخلفيتها فارغة:
`blur(40px)` مضبوط عليها ولا يفعل شيئاً، فيبقى لونها الشفّاف طبقة شاحبة
فوق نصّ حادّ.

هذه العلّة أصابت كل سطح زجاجي داخل سطح زجاجي:

| السطح | قبل | بعد |
|---|---|---|
| قائمة المستخدم | داخل الترويسة | بوّابة إلى `body` |
| بحث لوحة الإدارة | داخل الشريط العلوي | بوّابة إلى `body` |
| نوافذ الإدارة | داخل طبقة التعتيم المموّهة | التعتيم صار طبقة شقيقة |

اللوحتان المعلّقتان تُعرضان الآن عبر بوّابة في `body`، ويحسب موضعهما
`useAnchoredPanel` من مستطيل الزرّ مع إبقائهما داخل الشاشة — والحساب لازم
لأنهما فقدتا مرجعهما النسبي بخروجهما.

### شفافية أعلى

`glass-strong` — تعتيم **0.44** بدل 0.58، وتمويه **64px** بدل 40px. يشمل
القوائم المنسدلة والأدراج والنوافذ وبطاقة المصادقة. ودُكِّن نصّ القوائم
إلى `ink-800` ليصمد فوق بطل المنحة الكحلي.

## v1.6.1 — إصلاح قائمة الجوال المنحصرة في الترويسة

**2026-09-17**

كانت قائمة الجوال تفتح داخل شريط الترويسة بدل أن تغطّي الشاشة: روابطها
تطفو فوق محتوى الصفحة بلا خلفية، ونصفها مقصوص.

العلّة أن عنصراً فيه `backdrop-filter` يصير **المرجع** لكل `position: fixed`
بداخله، لا نافذة المتصفّح. والدرج كان مكتوباً داخل `<header>` الزجاجية،
فانحصر `fixed inset-0` في صندوقها: **388×64** بدل **390×780**.

العلّة سابقة لتغيير شفافية الترويسة: `glass-strong` القديمة تحمل
`backdrop-filter` أيضاً، وقياسها يعطي 388×64 نفسها. رفع الشفافية أظهرها
لا أكثر.

الإصلاح: أُخرج الدرج من `<header>` ليصير شقيقاً لها لا ابناً، مع تعليق
يشرح السبب حتى لا يُعاد داخلها.

فُحصت بقية الطبقات العائمة فوجدت سليمة — درج فلاتر المنح، ودرج قائمة
الإدارة، وقائمة المستخدم المنسدلة — لأن أيّاً منها ليس داخل عنصر مموّه.

## v1.6.0 — تنسيق الواجهة على الهاتف

**2026-09-17**

فحصٌ لعشرين صفحة على عرض 375px و320px كشف تمريراً أفقياً وقائمة إدارة
لا تُفتح أصلاً على الهاتف.

### تمرير أفقي كان يجرّ الصفحة كلها

`min-width: auto` — القيمة الافتراضية لعنصر الشبكة — تمنعه من النزول
تحت عرض محتواه الأدنى. فصفٌّ فيه عنوان منحة طويل كان يمدّ بطاقته إلى
528px داخل شاشة 375px، ويمدّ معها الصفحة إلى **545px**. أُضيف `min-w-0`
إلى `Card` وإلى عمود لوحة الطالب، ولُفَّ رأس البطاقة (`CardHeader`)
فلا يتزاحم عنوان طويل مع زرّ فعل. الصفحات العشرون تقف الآن عند عرض
الشاشة بلا تمرير أفقي على 375px و320px معاً.

### لوحة الإدارة على الهاتف

كانت قائمتها `hidden lg:flex` بلا أي بديل: من يفتح `/admin` من هاتفه
لا يملك وسيلة للانتقال إلى أي قسم ولا لتسجيل الخروج. صارت تُفتح درجاً
منزلقاً بزرّ في الشريط العلوي، يُغلق بالنقر خارجه أو بـ Esc أو بفتح
صفحة منه.

### جداول الإدارة

جدول بعرض 720px في شاشة 375px يعني أن نصف الأعمدة خارج الرؤية بلا ما
يدلّ عليها — وأعمدة الحالة والإجراءات أوّل ما يغيب. صار `DataTable`
يعرض كل صفّ بطاقةً بعناوين الأعمدة إلى جانب قيمها دون الشاشات
المتوسّطة، والجدول كما هو فوقها.

### كثافة وتناسق

- قائمة لوحة الطالب تُطوى على الهاتف: بطاقة المستخدم أفقية، والأقسام
  شريط يُمرَّر جانبياً — بدل عمود بطول الشاشة يسبق المحتوى في كل صفحة.
- بطاقات الأرقام عمودان بدل عمود واحد، وتسمياتها تُلَفّ على سطرين بدل
  أن تُقصّ في منتصف الكلمة.
- زرّا البطل في الصفحة الرئيسية يمتدّان بعرض الشاشة بدل صفٍّ متعرّج.
- «ترتيب حسب» تُطوى على الهاتف: كانت تُزاحم زرّ الفلاتر وتُلَفّ سطرين.
- مناطق لمس أكبر: روابط التذييل وأيقونات التواصل وتبويبات الإشعارات
  كانت بين 15px و32px، وصارت 36–40px.

## v1.5.1 — رأس الصفحة زجاج أعلى شفافية

**2026-09-17**

كانت الترويسة تستخدم `glass-strong` نفسه المخصّص للقوائم المنسدلة
والنوافذ، فبدت لوحاً معتّماً لا زجاجاً. صار لها سطحها الخاص.

### السطح

- `glass-header` جديد: تعتيم **0.34** بدل 0.58، وتمويه **72px** بدل 40px.
  خفض التعتيم وحده يجعل ما يمرّ تحت الترويسة مقروءاً فيزاحم نصّ القائمة؛
  التمويه العالي يذيبه إلى لون واحد فيبقى الإحساس الزجاجي ويهدأ الخلف.
- السطوع عند **1.12** فقط. رفعه أكثر كان يُشبع خلفية الصفحة الفاتحة
  إلى أبيض كامل — أي يُطفئ الشفافية المطلوبة من حيث أُريد تحسينها.
- ظلّ أخفّ من ظلّ `glass-strong`: الترويسة ملتصقة بالحافة فلا تحتاج
  إيحاء الارتفاع نفسه.
- طُبِّق على ترويسة الموقع وشريط لوحة الإدارة. القائمة الجانبية للجوال
  والقوائم المنسدلة بقيت على `glass-strong`: هي تطفو فوق المحتوى وتحجبه
  عمداً.

### التباين

الترويسة تمرّ فوق بطل اللاندينج الكحلي، والرمادي المتوسّط كان يضيع عليه
حتى قبل هذا التغيير. فدُكِّنت نصوص الترويسة:

- روابط التنقّل من `ink-500` إلى `ink-800`، والنشط ومؤشّره إلى `navy-900`.
- جرس الإشعارات إلى `ink-700`، وزرّ قائمة الجوال و«تسجيل الدخول» إلى `ink-800`.
- بدائل `@supports not (backdrop-filter)` و`prefers-reduced-transparency`
  تشمل السطح الجديد.

## v1.5.0 — تفعيل الحساب برابط بدل رمز

**2026-09-17**

كان المستخدم ينسخ رمزاً من ستة أرقام من بريده ويكتبه في الموقع. صار
يضغط زراً واحداً في الرسالة فيُفعَّل حسابه ويدخل مباشرة.

### الرابط

- رمز عشوائي بطول **64 محرفاً** يُمرَّر في الرابط ويُخزَّن مُجزَّأً. طوله
  يجعل تخمينه غير عملي، فسقط عدّاد المحاولات الذي كان ضرورياً للرقم
  القصير.
- صالح **48 ساعة** لا 15 دقيقة: الرابط يُفتح من صندوق البريد وقد لا
  يُفتح فوراً.
- يُستخدم مرة واحدة، وإعادة فتحه بعد التفعيل تقود إلى تسجيل الدخول لا
  إلى رسالة خطأ.
- `FRONTEND_URL` صار مطلوباً — يُبنى منه الرابط داخل الرسالة.
- استعادة كلمة المرور **لم تتغيّر**: ما زالت برمز من ستة أرقام، فهي
  عملية يُطلب فيها إثبات الحضور لا مجرّد فتح رابط.

### تصميم الرسالة

- ترويسة كحلية تحمل شعار المنصّة (PNG — عملاء البريد لا يعرضون SVG).
- زر التفعيل مبنيّ كخلية جدول ملوّنة، وهي الصيغة التي تعرضها كل عملاء
  البريد بما فيها Outlook.
- صندوق احتياطي يحمل الرابط نصّاً لمن لا يعمل عنده الزر، بجهة `ltr`
  وكسر داخل الكلمة لأن الرابط لاتيني طويل داخل قالب عربي.
- نسخة نصية مرافقة للنسخة المرئية.

## v1.4.0 — الشعار، والخدمة اليدوية، وآخر تسجيل دخول

**2026-09-17**

### شعار المنصّة

- الشعار مرسوم **متجهاً (SVG)** لا صورة نقطية: يكبر بلا تحبّب، وحجمه
  بالبايتات، ويتلوّن أبيض على الخلفيات الداكنة دون ملف ثانٍ.
- `logo.svg` و`favicon.svg` **يُولَّدان من مكوّن React نفسه**، فلا يتفرّق
  الثلاثة عند أي تعديل لاحق.
- أيقونة التبويب صارت مربّعاً كحلياً بعلامة بيضاء — أوضح من خطّ رفيع
  عند 16 بكسل. وحُذفت الأيقونة الافتراضية المتروكة في المشروع.

### فصل المسار الآلي عن اليدوي

صفحة الأدوات صارت مسارين صريحين:

| المسار | من ينفّذه | النتيجة |
|---|---|---|
| **فوري** | الذكاء الاصطناعي | نصّ خلال ثوانٍ من بيانات الملف الأكاديمي |
| **يدوي** | فريق منحتي | ملف نهائي يُسلَّم خلال 24–48 ساعة |

كلاهما مجاني. بوابة الدفع مؤجَّلة.

### الخدمة اليدوية من طرف إلى طرف

- ثلاث خدمات: كتابة سيرة من الصفر، تحسين سيرة، تحسين خطاب دافع.
- الطالب يرفع ملفه (PDF/Word) مع ملاحظاته، والفريق يحمّله من لوحة
  الإدارة، يعمل عليه، ثم يرفع النسخة النهائية فيصل الطالب إشعار بالتحميل.
- **الملفات على قرص خاص لا عام**: مستندات شخصية لا يجوز الوصول إليها
  برابط مباشر، فتُخدَّم عبر مسار يتحقّق من الملكية.
- **المنع لكل خدمة على حدة**: طلب خطاب قائم لا يحجب طلب سيرة — كانا
  يتحاجبان لأن الشرط كان على وجود أي طلب نشط.
- استبدال ملف مُسلَّم يحذف سابقه فلا تتراكم نسخ مهجورة على القرص.

### لوحة الإدارة

- عمود **آخر دخول** بالتاريخ والساعة، و«لم يدخل بعد» لمن لم يسجّل دخوله.
- جدول الطلبات أُعيد ترتيبه في ستة أعمدة بعد أن كانت سبعة تفيض وتقصّ
  عمود الأزرار.
- حسابات التشغيل صارت تحمل **اسم دورها** (Admin، Moderator، Expert،
  Student) لا أسماء أشخاص — هي حسابات نظام يتشاركها الفريق، واسم شخص
  عليها يضلّل من يقرأ سجلّ الإجراءات. ترحيل يصحّح الحسابات القائمة.

## v1.3.0 — البريد من حساب Gmail بلا SMTP

**2026-09-17**

أظهر سجل الإنتاج فشل كل رسائل التحقق:
`Unable to connect to smtp.gmail.com:2587 (Connection timed out)`. فيه خطآن
متراكبان: المنفذ 2587 خاص بـ Resend ولا يقدّمه Gmail أصلاً، وحتى بالمنفذ
الصحيح تحجب Railway وRender منافذ SMTP الصادرة. الحل الدائم ناقل يعمل عبر
HTTPS — وGmail يتيح ذلك عبر واجهته.

### ناقل `gmail` عبر Gmail API

- ناقل بريد جديد يرسل من حساب Gmail عبر HTTPS (المنفذ 443 الذي لا يُحجب)،
  بمصادقة OAuth 2.0 برمز تحديث دائم — لا كلمة مرور تطبيق ولا نطاق.
  الرسائل تصل بتوقيع Google نفسه، وهو أفضل تسليم ممكن بلا نطاق.
- رمز الوصول يُخزَّن مؤقتاً حتى قبيل انتهائه، ويُجدَّد مرة واحدة إن رفضته
  Google قبل ذلك. مبني على `Http` بلا أي اعتمادية جديدة.
- ترويسة Bcc تُعاد إلى النص المُرسَل لأن Gmail يقرأ المستلمين منها،
  بينما يحذفها Symfony لصالح مغلّف SMTP.
- **`php artisan menhity:gmail-auth`**: يربط الحساب مرة واحدة من جهاز
  المطوّر — يفتح شاشة موافقة Google، يستقبل عودة المتصفح على منفذ محلي،
  ويطبع المتغيّرات جاهزة للّصق في لوحة الاستضافة. يقبل لصق الرابط يدوياً
  حيث لا يصل المتصفح إلى الطرفية (`--manual`).

### تشخيص يسمّي السبب بدل العرَض

- **فحص المنفذ قبل الإرسال** في `menhity:mail-test`: اتصال TCP خلال خمس
  ثوانٍ يميّز المنفذ الخاطئ عن المنفذ المحجوب، ويثبت أن HTTPS يعمل حين
  يكون SMTP هو المحجوب — بدل انتظار مهلة الإرسال كاملة.
- **جدول منافذ المزوّدين**: منفذ لا يقدّمه المضيف (كـ 2587 مع Gmail) يُسمّى
  في الخطأ نفسه مع المنافذ الصحيحة له.
- **سجل الخادم صار يحمل حقل `hint`** بالخطوة العملية بجانب كل فشل إرسال،
  وأخطاء Gmail OAuth (`invalid_grant`، `invalid_client`، واجهة معطّلة)
  مترجمة إلى خطوات كما أخطاء SMTP.

### الواجهة لا تنتظر بريداً لن يصل

- استجابة التسجيل (والدخول بحساب غير مؤكَّد) تحمل `code_sent`: حين يفشل
  المزوّد تعرض شاشة التأكيد تنبيهاً بأن الرسالة لم تُرسَل وأن الخلل من
  جهة المنصّة، ويصبح زر «إعادة الإرسال» متاحاً بعد ١٥ ثانية بدل ٦٠.
- مسارا «نسيت كلمة المرور» و«إعادة الإرسال» يحتفظان برسالتهما الموحّدة
  حتى لا يُستدلّ منهما على وجود الحساب.

### التوثيق

- قسم البريد في `DEPLOYMENT.md` صار يبدأ بجدول قرار: نطاق → Resend،
  حساب Gmail بلا نطاق → Gmail عبر HTTPS، وSMTP فقط حيث لا يُحجب — مع
  خطوات Google Cloud كاملة وتنبيه أن شاشة الموافقة في وضع Testing تُنهي
  رمز الربط بعد سبعة أيام.

---

## v1.2.0 — الثيم الزجاجي

**2026-09-17**

إعادة تصميم كاملة للواجهة على أسطح زجاجية شبه شفّافة. العمل جرى على
مستوى التوكنز والمكوّنات المشتركة، فانتقل الأثر إلى كل الصفحات دون
تعديل كل صفحة على حدة.

### الطبقة الزجاجية

- **خلفية محيطة**: ستّ بقع لونية ثابتة `fixed` تحت المحتوى. بلا تنوّع
  خلف السطح لا يظهر للتمويه أثر، ويُقرأ اللوح أبيض مسطّحاً.
- **أربع درجات**: `glass` للمحتوى، و`glass-strong` لما يطفو فوقه
  (الترويسة والقوائم المنسدلة)، و`glass-soft` للأسطح المتداخلة،
  و`glass-dark` للألواح الكحلية، و`glass-gold` للعنصر النشط والفعل
  الأساسي.
- الإحساس الزجاجي من ثلاثة معاً: **حافة علوية مضيئة** (`inset`)،
  **لمعة منزلقة** عبر السطح، و**تمويه 30px بتشبّع 190%**.

### قواعد تباين ظهرت أثناء العمل

الصبغة الشفّافة فوق خلفية معاكسة الإضاءة **تنزاح لونياً**:

| الحالة | الأثر | المعالجة |
|---|---|---|
| كحلي مخفّف فوق أرضية فاتحة | يبهت إلى رمادي | البدء من `navy-900` ليصل بعد التخفيف إلى كحلي المنصّة |
| ذهبي مخفّف فوق ترويسة كحلية | ينزاح إلى بنّي | رفع التعتيم إلى 84% — الإحساس من اللمعة لا الشفافية |
| حقول بيضاء شفّافة فوق سطح داكن | نصّ غير مقروء | زجاج معكوس: سطح أبيض شفّاف ونصّ أبيض |

- نسخة `onDark` للأزرار والشارات فوق الترويسات الداكنة.
- تغميق `ink-400`: النص الباهت فوق سطح زجاجي يفقد تباينه.
- الأزرار الرئيسية بقيت صلبة — الزجاج يخفض التباين، والفعل الأساسي
  يجب أن يُرى فوراً.

### الأداء وإتاحة الوصول

- التمويه على الأسطح الكبيرة وحدها؛ الشارات تعتمد الشفافية فقط.
- بلا دعم `backdrop-filter` يرتفع التعتيم إلى 96% فيبقى النص مقروءاً.
- `prefers-reduced-transparency` يعطّل الشفافية واللمعة معاً.

## v1.1.0 — تأكيد البريد ومزوّد ذكاء اصطناعي مجاني

**2026-09-15**

### تأكيد البريد الإلكتروني عند التسجيل

إنشاء الحساب صار يمرّ بخطوة تأكيد: يصل رمز من ستة أرقام إلى بريد المستخدم،
ولا تُفتح الجلسة قبل إدخاله.

- رسالة بريد عربية RTL بهوية المنصّة، ترسل رمزاً صالحاً **١٥ دقيقة**.
- الرمز يُخزَّن مُجزَّأً (hash)، ويُلغى السابق مع كل إرسال جديد،
  ويُقفَل بعد **٥ محاولات** خاطئة.
- الدخول بحساب غير مؤكَّد يُعيد `409` مع رمز جديد، وتنقل الواجهة المستخدم
  إلى شاشة التأكيد تلقائياً.
- نجاح التأكيد يسلّم توكن الجلسة مباشرة — لا حاجة لتسجيل دخول منفصل.
- استعادة كلمة المرور صارت **ترسل بريداً فعلياً** بدل طباعة الرمز في السجل،
  وتؤكّد البريد ضمناً لأن صاحبه أثبت ملكيته.
- الحسابات المنشأة قبل هذا التحديث تُعتبر مؤكَّدة بترحيل قاعدة بيانات،
  فلا يُحجب أحد عن حسابه.
- **مطلوب عند النشر:** ضبط متغيّرات `MAIL_*` — الخطوات في
  [DEPLOYMENT.md](DEPLOYMENT.md#ربط-البريد-الإلكتروني-مجاناً).

### Cloudflare Workers AI مزوّداً مجانياً

- مزوّد جديد ينفّذ واجهة `AiProvider` نفسها: حصة يومية مجانية، بلا بطاقة
  دفع وبلا قيود جغرافية.
- يتصدّر ترتيب الاختيار التلقائي، و`AI_PROVIDER` ما زال يتقدّم عليه.
- اسم النموذج من `CLOUDFLARE_MODEL`، فتغييره لا يحتاج تعديل كود.

### إرسال البريد عبر HTTPS

تحجب أغلب الاستضافات — Railway منها — منافذ SMTP الصادرة (25 و465 و587)
لمنع السبام، فيفشل كل إرسال بمهلة اتصال مهما صحّ الإعداد.

- ناقل بريد جديد يرسل عبر **واجهة Resend على HTTPS** (المنفذ 443) الذي
  لا يُحجب. مبني على `Http` بلا أي اعتمادية جديدة.
- التفعيل بمتغيّرين: `MAIL_MAILER=resend` و`RESEND_API_KEY`.

### إصلاح نسبة اكتمال الملف في لوحة الطالب

- النسبة في الشريط الجانبي كانت **تتجمّد** حتى إعادة تحميل الصفحة: تخطيط
  اللوحة أبٌ للمسارات فلا يُفكّ تركيبه عند التنقّل، وكان يجلب النسبة مرة
  واحدة بلا تبعيات. صارت تُقرأ من سياق المصادقة، فتتحدّث فور أي حفظ.
- ويزرد السيرة الذاتية يعدّل الملف الأكاديمي أيضاً، وكان لا يُحدِّث
  السياق بعد الحفظ — صار يفعل.

### عنوان الردّ في الرسائل

- `MAIL_REPLY_TO_ADDRESS`: عنوان المُرسِل مقيَّد بنطاق موثَّق لدى المزوّد،
  أما عنوان الردّ فأي بريد يصلح — فترسل المنصّة باسمها وتصل الردود إلى
  بريد حقيقي.

### تحسين تسليم البريد

- رسائل التحقق صارت تحمل **نسخة نصية** إلى جانب المرئية. الرسالة
  بصيغة HTML وحدها إشارة تصنيف لدى مزوّدي البريد.

### أدوات

- **`php artisan menhity:mail-test <بريد>`**: يطبع إعداد البريد الفعّال
  (مع تنبيه إن احتوت كلمة المرور مسافات) ثم يرسل رسالة حقيقية، ويترجم
  أخطاء SMTP الشائعة إلى خطوة عملية. لا يلمس قاعدة البيانات.

### إصلاحات

- **إعداد بريد خاطئ كان يعلّق الطلب بلا نهاية**: رسالة التحقق تُرسَل
  داخل طلب التسجيل والدخول نفسه، و`config/mail.php` كان بلا مهلة
  (`timeout => null`) — فخادم SMTP لا يستجيب يوقف الطلب للأبد، ويبقى
  زر «جارٍ المعالجة…» يدور في الواجهة. صارت المهلة 15 ثانية
  (`MAIL_TIMEOUT`)، ولعميل axios مهلة 30 ثانية برسالة عربية واضحة —
  وتشغيل أدوات الذكاء الاصطناعي له مهلة أطول لأنه يستغرق دقيقة.
- **`MAIL_SCHEME=tls` في التوثيق كانت قيمة غير مقبولة**: Symfony Mailer
  يقبل `smtp` أو `smtps` فقط، فكان كل إرسال يفشل لمن اتبع الجدول.
- **رسائل أخطاء الحقول كانت تظهر بحرف واحد**: `parseApiError` كانت تحوّل
  مصفوفة الرسائل إلى نص، بينما تقرأها المكوّنات بصيغة `[0]` — فيظهر أول
  حرف من الرسالة بدلاً منها.
- **`last_login_at` لم يكن يُحفظ**: الحقل خارج `$fillable`، فكان
  `update()` يتجاهله بصمت وتبقى خانة «آخر دخول» فارغة في لوحة الإدارة.

---

## v1.0.0 — الإصدار الأول

**2026-09-14**

الإصدار الأول الكامل لمنصة **منحتي**: خادم Laravel 13 يقدّم API، وواجهة React 19
منفصلة، وقاعدة PostgreSQL 16 — بواجهة عربية RTL بالكامل.

> **قبل النشر**: المنصة تعمل بالكامل مقابل قاعدة بيانات حقيقية، لكن ثلاثة تكاملات
> خارجية تحتاج بيانات اعتماد عند النشر (البريد الإلكتروني، دخول Google/Apple،
> التخزين السحابي). التفاصيل في
> [ما يحتاج ربطاً خارجياً](#ما-يحتاج-ربطاً-خارجياً-قبل-الإنتاج) أدناه.

---

### ما في هذا الإصدار

#### الخادم — Laravel 13 · PHP 8.4
- **66 مساراً** تحت `/api/v1`، كلها تُعيد JSON.
- **31 جدولاً للمجال** في ثمانية ترحيلات، و**31 نموذج Eloquent**.
- **14 تعداداً** بترجمة عربية لكل قيمة.
- مصادقة **Laravel Sanctum**: توكن مستقل لكل جهاز، مع أعمدة الجهاز (المتصفح،
  النظام، نوع الجهاز، عنوان IP) فتصبح كل جلسة قابلة للعرض والإبطال منفردة.
- استعادة كلمة المرور برمز من ٦ أرقام، بصلاحية ١٥ دقيقة وحدّ ٥ محاولات.
- ثماني خدمات منطق أعمال: المطابقة، اكتمال الملف، تقرير ATS، الذكاء الاصطناعي،
  طلبات السيرة الذاتية، استعلام المنح، تمييز الأجهزة، وسجل العمليات.
- **39 اختباراً · 129 تأكيداً** تمرّ كلها.

#### الواجهة — React 19 · React Router 7 · Vite 8 · Tailwind v4
**34 مساراً** تعمل كلها مقابل الـ API الحقيقي:

| المنطقة | المحتوى |
|---|---|
| الزائر | اللاندينج، تصفّح المنح بالفلاتر والبحث والترتيب، تفاصيل المنحة، الأدوات، من نحن، تواصل، الأسئلة الشائعة، مركز المساعدة، الشروط، الخصوصية، 404 |
| المصادقة | الدخول، إنشاء الحساب، نسيت كلمة المرور، إدخال الرمز، كلمة مرور جديدة، تأكيد التغيير |
| الطالب | نظرة عامة، الملف الأكاديمي بسبعة أقسام قابلة للتحرير، المحفوظات، المستندات، الإشعارات، الإعدادات |
| الأدوات | مركز الأدوات، مشغّل الأدوات التحليلية الأربع، ويزرد السيرة الذاتية بخمس خطوات، متابعة طلب الخبير |
| الإدارة | ثمانية أقسام: النظرة العامة، المنح، المستخدمون، الأدوات، الطلبات، الإشعارات، التقارير، الإعدادات |

- فلاتر المنح محفوظة في عنوان الصفحة فتكون قابلة للمشاركة، وزر الرجوع يعمل كما يُتوقّع.
- لوحة الإدارة تُحمَّل عند الطلب (`lazy`) فلا يحمّلها الطالب ولا الزائر.
- كل الرسوم البيانية مكتوبة بـ SVG مباشرة بلا مكتبة خارجية، بتسميات مباشرة
  وفجوات ٢px كترميز ثانوي فلا يعتمد القارئ على اللون وحده.
- كل المشاهد التوضيحية SVG مكتوبة يدوياً بدل الصور الفوتوغرافية.

#### إرسال البريد عبر HTTPS

تحجب أغلب الاستضافات — Railway منها — منافذ SMTP الصادرة (25 و465 و587)
لمنع السبام، فيفشل كل إرسال بمهلة اتصال مهما صحّ الإعداد.

- ناقل بريد جديد يرسل عبر **واجهة Resend على HTTPS** (المنفذ 443) الذي
  لا يُحجب. مبني على `Http` بلا أي اعتمادية جديدة.
- التفعيل بمتغيّرين: `MAIL_MAILER=resend` و`RESEND_API_KEY`.

### إصلاح نسبة اكتمال الملف في لوحة الطالب

- النسبة في الشريط الجانبي كانت **تتجمّد** حتى إعادة تحميل الصفحة: تخطيط
  اللوحة أبٌ للمسارات فلا يُفكّ تركيبه عند التنقّل، وكان يجلب النسبة مرة
  واحدة بلا تبعيات. صارت تُقرأ من سياق المصادقة، فتتحدّث فور أي حفظ.
- ويزرد السيرة الذاتية يعدّل الملف الأكاديمي أيضاً، وكان لا يُحدِّث
  السياق بعد الحفظ — صار يفعل.

### عنوان الردّ في الرسائل

- `MAIL_REPLY_TO_ADDRESS`: عنوان المُرسِل مقيَّد بنطاق موثَّق لدى المزوّد،
  أما عنوان الردّ فأي بريد يصلح — فترسل المنصّة باسمها وتصل الردود إلى
  بريد حقيقي.

### تحسين تسليم البريد

- رسائل التحقق صارت تحمل **نسخة نصية** إلى جانب المرئية. الرسالة
  بصيغة HTML وحدها إشارة تصنيف لدى مزوّدي البريد.

### أدوات الذكاء الاصطناعي
خمس أدوات تقرأ الملف الأكاديمي تلقائياً كسياق: إنشاء السيرة الذاتية، تحسينها،
إنشاء خطاب الدافع، تحسينه، وتقييم الملف الشخصي.
بدون `ANTHROPIC_API_KEY` تعمل بوضع محاكاة يُظهر تنبيهاً واضحاً بدل أن تفشل.

**خدمة صياغة السيرة الذاتية** مجانية بالكامل وتمرّ بمسار: إرسال ← إسناد تلقائي
لأقلّ الخبراء انشغالاً ← تايملاين بأربع مراحل ← تقرير ATS ← تسليم، مع مراسلة الخبير.

---

### الأمان
- كلمات المرور مشفّرة بـ bcrypt (١٢ جولة).
- تغيير كلمة المرور يُنهي الجلسات على الأجهزة الأخرى، واستعادتها تُنهيها جميعاً.
- إيقاف الحساب من لوحة الإدارة يمنع الدخول عبر وسيط `active`.
- المستندات تُخزَّن بأسماء عشوائية، ويُتحقَّق من الصيغة والحجم قبل القبول.
- كل متحكّم يتحقّق من ملكية العنصر قبل التعديل أو الحذف.
- مسارات الإدارة محمية بوسيط `role:admin,moderator`، والحذف وتغيير الأدوار للمدير فقط.

---

### ما يحتاج ربطاً خارجياً قبل الإنتاج

| الميزة | الحالة الآن | المطلوب |
|---|---|---|
| إرسال البريد الإلكتروني | رموز التحقق تُطبع في سجل الخادم | ربط مزوّد بريد (Resend / SES / SMTP) |
| دخول Google / Apple | الأزرار موجودة وتعرض رسالة توضيحية | إضافة بيانات اعتماد OAuth |
| توليد الذكاء الاصطناعي | وضع المحاكاة | إضافة `ANTHROPIC_API_KEY` |
| تخزين الملفات | قرص `public` المحلي | نقله إلى تخزين سحابي (S3 / R2) |
| أرشفة المنح المنتهية | تحديث يدوي من لوحة الإدارة | إضافة مهمة مجدولة |

---

### التشغيل

راجع [README](README.md). باختصار:

```bash
# الخادم
cd backend && composer run setup && php artisan serve

# الواجهة (طرفية ثانية)
cd frontend && npm install && npm run dev
```

حسابات تجريبية بكلمة المرور `Menhity@2026`:
`admin@menhity.com` · `moderator@menhity.com` · `expert@menhity.com` · `student@menhity.com`

---

### ملاحظات

- التضاربات التي ظهرت في ملف التصاميم وكيف حُسمت، والأخطاء البرمجية التي اكتُشفت
  أثناء الفحص وصُحّحت — موثّقة كلها في [docs/DESIGN-DECISIONS.md](docs/DESIGN-DECISIONS.md).
- هذا الإصدار يستبدل نسخة Next.js الأولى بالكامل، بناءً على الستاك المطلوب:
  HTML/CSS/JS/React للواجهة و PHP/Laravel للخادم.
